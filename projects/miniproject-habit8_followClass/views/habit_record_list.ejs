<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>메모 목록</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
      tr {
          text-align: center;
      }
  </style>
</head>
<body>
  <%- include("./nav.ejs") %>

  <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column"> 
      <h2><%= name %>님 어서오세요!</h2>
      <!-- (2) / 안 붙이면 localhost:3000/record_add 아니고 /record/record_add -->
      <!--      (1)  이때 action="/record_add/id" == app.get("/record_add/:id") 같아야함 -->
      <!-- <form method="get" action="/record_add/<%= habit_id %>" >
        <button type="submit">메모 추가</button> 
      </form> -->
      <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <button class="btn btn-success" type="button" onclick="location.href='/record_add/<%= habit_id %>'">메모 추가</button>
      </div>

      <table class="table table-hover">
          <thead>
            <tr>
              <th>아이디</th>
              <th>습관 기록 메모</th>
              <th>기록 생성일</th>
              <th>삭제</th>
            </tr>
          </thead>
          <tbody class="table-group-divider">
            <%
            records.forEach(record=> {
            %>
            <tr>
                <td><%= record.id %></td>
                <td><%= record.memo %></td>
                <td><%= record.createdAt %></td>
                <td><a href="/delete/<%= habit_id %>/<%= record.id %>" style="text-decoration:none; color:#32762c;">삭제</a></td>
            </tr>
            <% 
            }); 
            %>
          </tbody>
      </table>
    </div>
</body>
</html>