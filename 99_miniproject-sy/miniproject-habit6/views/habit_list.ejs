<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>습관 목록</title>
</head>
<body>
  <nav>
    <a href="/habit_list">Home</a>
    <a href="/habit_list">습관관리</a>
    <a href="/register">회원가입</a>
    <a href="/logout">로그아웃</a>
  </nav>
  
  <h2><%= name %>님 어서오세요!</h2>

    <button type="button" onclick="location.href='/habit_add'">습관 추가</button>

    <table border="1">
        <thead>
          <tr>
            <th>아이디</th>
            <th>습관명</th>
            <th>시작일 ~ 종료일</th>
            <th>기록갯수</th>
            <th>삭제</th>
          </tr>
        </thead>
        <tbody>
          <%
          habits.forEach(habit=> {
          %>
          <tr>
              <td><%= habit.id %></td>
              <td><a href="/record/<%= habit.id %>" style="text-decoration:none;"><%= habit.habit_name %></a></td>
              <td><%= habit.start_dt %> ~ <%= habit.end_dt %></td>
              <td><%= habit.count %></td>
              <td><a href="/delete/<%= habit.id %>" style="text-decoration:none;">삭제</a></td>
          </tr>
          <% 
          }); 
          %>
        </tbody>
    </table>
</body>
</html>